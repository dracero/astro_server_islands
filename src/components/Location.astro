
---
// Location.astro

// Función para obtener la ubicación
export async function getIsInLanus() {
  const response = await fetch('https://ipapi.co/json/');
  const data = await response.json();
  return data.city && data.city.toLowerCase() === 'lanús';
}

---

